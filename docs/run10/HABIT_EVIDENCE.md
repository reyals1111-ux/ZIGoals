# Bounded Habit rule and timer evidence — Run 10
Scope: local Habit changes after parent checkpoint `10d89d1`; source backups in ignored `.superpowers/run10/recovery/habits/`. No deployment, commits, financial execution or additional workstreams by this agent.
HAB-03: Habit editor accepts an explicit future effective day; normal edit/pause/resume/archive paths preserve today and earlier rules. Current cards, quick logging, filtering and streak units use the rule applicable today, not the newest future rule. Descriptive fields update immediately; changed interval schedules retain their established anchor.
Rule evidence: optional append-only `ruleRevisions` retains original available rules and every scheduled edit, including superseded terms for the same future day. Stale editor fingerprints reject changed rules; existing stored rule history is copied exactly without inventing earlier same-day versions. UI displays scheduled boundaries and retained revisions.
Future rules must start tomorrow or later and on/after the latest pending boundary; replacing that future day retains the superseded revision. Mixed natural target periods retain existing neutral/partial semantics rather than forcing a failed period. Legacy helper contracts remain compatible; this is not a redesign of all historical statistics.
HAB-05: optional saved duration timer records stable ID, start/segment/pause timestamps, original date, IANA timezone, unit and rule fingerprint. Elapsed time comes from timestamps, including time away; no forced minimum minute. Running timers survive reload, pause/resume retains measured segments, and reviewed logging atomically adds one value plus an idempotent receipt.
Timer safety: stale reviews reject changed timer segments; reused identities cannot restart or resurrect discarded timers. Clock reversal, incompatible rule changes, spans over seven days and cross-midnight spans require review. Cross-day timers remain visible with original evidence; users record each day through existing History correction and explicitly discard the timer. No automatic split, native/background alarm or push capability is claimed.
HAB-06: existing calendar, notes/mood, manual correction, type/recurrence templates and keyboard completion remain; timer receipts and rule history add readable evidence. Pause/archive/restore buttons now schedule from the displayed future date, preserving recorded current-day outcomes. Two legacy browser journeys were updated for this intentional behavior.
Unit proof: `PATH=/Users/AIUSER/.local/share/fnm/node-versions/v24.19.0/installation/bin:$PATH pnpm exec vitest run apps/web/lib/habit-actions.test.ts apps/web/lib/habits.test.ts apps/web/lib/habits-beta.test.ts apps/web/lib/dashboard-metrics.test.ts apps/web/lib/life-dashboard-ui.test.ts` — 45 passed; nine new action tests cover future units/state, retained originals, stale edits, timer replay/discard, JSON restore, DST, midnight, clock reversal, malformed timestamps and stale timer review.
Browser proof: `PATH=/Users/AIUSER/.local/share/fnm/node-versions/v24.19.0/installation/bin:$PATH PLAYWRIGHT_BASE_URL=http://127.0.0.1:3110 pnpm --filter @zigoals/web exec playwright test tests/run10-habit-actions.spec.ts tests/habits.spec.ts --workers=1 --output=/private/tmp/run10-habits-release-tests` — 18 passed (24.0s), desktop and emulated mobile, actual local private persistence. Initial missing controls failed before implementation.
Final guard proof: after the stale-review guard, the same browser command restricted to `tests/run10-habit-actions.spec.ts --output=/private/tmp/run10-habits-reviewed-timer-tests` — 4 passed (7.9s). Test clocks were fixed before creation and writes acknowledged before date changes; fixtures do not masquerade as real elapsed waiting.
Static/visual: full `pnpm typecheck`, scoped ESLint and `git diff --check` passed. Fictional `docs/run10/evidence/habit-future-timer-320-{desktop,mobile}.png` captured; mobile screenshot inspected and 320px overflow checked. No physical device or formal accessibility certification claimed.
Compatibility/capacity: original `zigoals:habits:v1` and schema v2 stay; old envelopes omit optional extensions unchanged. Existing limits remain 200 Habits / 2,000 effective rules / 20,000 day entries; new 4,000 revision and 10,000 timer-receipt caps reject writes rather than prune. Older strict application builds reject extension fields; retain forward backups before rollback.
Remaining HAB-03/04: cross-device rule merge/resolution is parent sync scope, not proved here; older contribution-Habit reconciliation still uses its legacy current-day helper and can require reviewing a pending future rule in Habits first. Goal-end automation and stacks remain explicit metadata, without funding or chained completion effects.
Remaining HAB-05/07/JRN-28: real independent-device timer races, account/device switching during a timer, timezone-move UI, encrypted multi-device timer replay and the entire mixed-cadence journey matrix remain unverified; automated reminders and cross-midnight allocation are unimplemented. This bounded local slice is evidenced, while these full acceptance IDs remain partial.
