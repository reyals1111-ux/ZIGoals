# Integrated account sync checkpoint
Email UI, server-only OTP/session adapter, account-scoped Storage/IndexedDB, memory-only vault key, encrypted chunk staging and CAS catalog are wired into Settings and the app provider. Private edits debounce; focus/30-second polling reconciles while unlocked; errors pause automatic attempts. Reload/idle/cross-tab identity changes lock. Local at-rest copies remain plaintext and are disclosed.
Independent Chrome profiles (1280px desktop and390px mobile emulation) exercised actual UI sign-in fixture, create/recovery acknowledgement, independent unlock, Habit creation, second-profile completion, first-profile receipt and locked reload against real persistent Miniflare Durable Objects and actual server adapter/WebCrypto. Stored ciphertext did not contain the fictional Habit name. This is NOT hosted email delivery or physical phone proof.
Command: `RUN10_BROWSER=1 PATH=/Users/AIUSER/.local/share/fnm/node-versions/v24.19.0/installation/bin:$PATH pnpm exec vitest run scripts/run10/account-browser.test.mjs` — 1 passed,5.50s. Standard suite intentionally skips this opt-in already-running-browser case; it was run separately. Fixture email/code and account identity are synthetic, not owner credentials.
The separate real Workers test passes tenant isolation, persisted restart, replay/CAS and retained deletion metadata; encrypted catalog tests add authenticated HEAD watermarks/fork rejection, >1MB staging with publication last, lost acknowledgement retry, consent-revoked pending Health refusal, first-attach conflicts and order-preserving merges —9 passed independently.
Server authenticates every request and rejects captured-account header mismatch; vault operations are scoped to the authenticated tenant and manifest identity. Captured local Storage views revoke after account generation changes. Account-wide Web Lock encloses capture/sync/apply/journal commit; every domain applies with local CAS, finances as one aggregate. Pending operations persist before network I/O.
Health defaults excluded; separate opt-in applies to upload and decryption/application. Revoking this tab's consent stops transfers and does not claim deletion of previous cloud copies. Independent helper review found and root fixed replay watermark, ordering, pending Health and cross-tab journal serialization gaps. See INDEPENDENT_REVIEW.md.
Combined local account/food/recovery browser checks:10 passed16.6s. Full unit integration:1295 passed,1 diagnostic harness failed because Settings now needs provider,1 opt-in browser skipped; harness now matches actual layout and its unchanged stale-account assertions pass (1/1 scoped). Final full verification still pending.
Initial browser fixture runs found an incorrect accessible button selector and exposed initially enabled account controls during hydration; selector corrected and initial account loading now disables controls. No evidence was relabelled as hosted success.
Remaining: hosted configuration/email/physical devices, account deletion/key-epoch rotation/device revocation UI, complete financial conflict resolution/export UI, retention/garbage collection and prolonged offline/key persistence acceptance. Immutable chunks retain history and refuse capacity exhaustion; no pruning claim. Domain local application is atomic per domain, not one transaction across all domains. Local outbox acknowledgement/compaction and full acceptance matrix remain incomplete.
