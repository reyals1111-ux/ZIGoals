# Market reliability implementation evidence
- The original deployed Alpha 503 cause remains unconfirmed. No provider calls, credentials, dashboard reads, deployment or production configuration change were made; historical 2026-09-22 entitlement/usage observations remain dated evidence, not a current balance.
- Reproduced a concrete client defect: a first pending catalog load had `catalogAt=0`, so a concurrent mount discarded the pending promise as expired. The existing creator browser test observed two requests instead of one. Pending work now has its own lifetime, separate from completed-cache freshness.
- `market-catalog-client.ts` shares initial and expired-cache pending work, validates the existing canonical asset reference boundary, retains TTL behavior and allows a later retry after failure; `asset-search.tsx` consumes the loader. Desktop/mobile creator regression now confirms exactly one catalog request.
- Added an isolated quote-only Durable Object adapter using real Workers storage transactions. Public work keys, generated operation IDs, configured endpoint cost, bounded queue/reservation/ownership/dispatch/settlement, matching lease generations and fenced publication persist atomically.
- Fresh normalized quote evidence is checked before new admission, retaining original source timestamps and exact values. Malformed/wrong-identity/older evidence cannot replace last-good; simultaneous equivalent work returns WAITING rather than sharing an owner-context I/O promise.
- One multi-key attempt has one configured cost. Duplicate enqueue for the same lease, duplicate settlement, dispatched cancellation, clock regression, expired ownership and late predecessor publication are denied. A storage failure grants no permission.
- Explicit required policy/month inputs have no production numerical defaults; expiry denies new reservations. Bounds are at most 128 retained attempt tombstones and 64 work keys, with separate persisted records. Capacity exhaustion fails closed; no pruning, replay-horizon reset or automatic uncertain-dispatch refund is implemented.
- Isolated config: `workers/market-coordinator/wrangler.local.jsonc`; public Worker fetch always returns 404. The internal namespace is used only by the runtime harness. No app route/provider imports the adapter, no provider I/O exists there, and no production binding was added.
- Test-first: catalog module and durable runtime test failed before implementations. Final focused market suite: 94 tests / 10 files passed; added catalog/durable unit subset: 9 tests / 2 files passed. Pinned Node 24.19.0 / pnpm 11.19.0.
- Real Miniflare/Workers SQLite test passed: two contenders/one concurrency slot, restart after dispatch, no refund, strict publication, duplicate settlement rejection, persisted cache, 20 warm hits without extra attempts, later restart and successor fencing, retained ambiguous dispatch charge. Outbound service throws on any attempted network call.
- Full TypeScript check and scoped lint passed; browser catalog regression 2/2 passed. Logs: `.superpowers/run10/market/{catalog-red,runtime-red,focused-final,unit-final,runtime-final,typecheck-final,lint-final,catalog-browser}.log`.
- Files: `apps/web/lib/market-catalog-client{.ts,.test.ts}`, `apps/web/components/platform/asset-search.tsx`, `apps/web/lib/server/durable-market-account{.ts,.test.ts}`, `workers/market-coordinator/{worker.ts,wrangler.local.jsonc}`, `scripts/run10/market-runtime.test.mjs`, this record. Existing-file backup: `.superpowers/run10/market/backup/asset-search.tsx`.
- Read: full `MASTER_PROMPT.md`, `RUN_9_MARKET_DATA.md`, `RUN_10_BETA_RELIABILITY_MASTER_PLAN.md`, `PRE_RUN_10_HARDENING.md`; canonical budget/coordinator/fence/breaker/provider adapter and service source, pair envelope and quote validation, their focused tests, current Alpha config and existing local Workers runtime-test pattern.
- Requirement disposition: MKT-01 partial/unconfirmed; MKT-02/03 existing boundaries preserved and regression-tested; MKT-04 dated evidence preserved; MKT-05/06/07/09/10/11/14 partial in the isolated quote adapter; MKT-08/12/13/15/17 deferred beyond this bounded slice; MKT-16 existing acceptance regressions pass; MKT-18 isolated configuration only. Remaining gates: connect all catalogs/history/insights/provider attempts to a shared backend; provider-dispatch/outcome integration; scheduler fairness/deadlines; durable breakers; public per-pair route/client migration; bounded eviction/replay horizon; verified reset policy/usage reconciliation; synthetic workload latency distributions; source-bound deployed-runtime 503 investigation and approved live acceptance. Global production quota protection is not claimed.
