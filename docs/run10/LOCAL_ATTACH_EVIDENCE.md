# Explicit local-to-account copy — verified local subset

The signed-in user can keep Local separate or choose Goals/Wealth, Habits, Health and/or Today settings. Health requires separate sync consent. Preview counts include nested object-array histories; unselected sections and Showcase/transaction journals are excluded. The app generates an encrypted selected-data backup and requires a saved-secret acknowledgment, download initiation and saved-file attestation before copying. Browser download initiation alone is not proof the user retained the file.

Confirmation rechecks account generation, Health consent, unchanged reviewed Local source and empty account/cloud destinations. Populated sections refuse replacement. All selected account records and outboxes now commit in one IndexedDB transaction under sorted locks. A later revision conflict or account-generation failure rolls back the batch. Empty migration pointers can survive; original Local records and migration recovery remain intact. Cloud publication/journal acknowledgment are separate from that local transaction. A competing remote writer can cause a normal sync conflict after the local copy; no server reservation is claimed.

Fresh actual two-profile app/auth-adapter/WebCrypto/IndexedDB/SQLite Worker browser journey passed16.89s: pre-existing Local Habit, email/vault flow, inventory, confirmation gates, encrypted download and decryption, copy to A and transfer to B, populated-destination refusal, four-domain round trip, offline water merge, revocation, cold lock, and signout returning to untouched Local original. Only upstream email identity is a fixture. No real inbox/physical phone acceptance. [Browser log](evidence/local-attach-browser.txt).

Focused vault suite passed; new transaction regressions prove later-domain conflict rolls back earlier writes/outbox, and fence call4 aborts after first-domain headers/outbox are staged. Fresh full unit invocation:1337passed,4failed due sandbox loopback denial,1explicit opt-in skip; only those4 Worker tests rerun with permission, all4passed. Combined distinct coverage1341passes/1skip, not a claim the first invocation was green. Types, affected lint and production build passed. [Full run](evidence/post-attach-units.txt), [permission rerun](evidence/post-attach-runtime.txt), [review](LOCAL_ATTACH_REVIEW.md).

Remaining acceptance: every selected combination/large history interruption in real browsers; hosted email and physical device; lifecycle deletion/rotation; explicit conflict-resolution and old pending-queue recovery UI. These stay inside Run10.
