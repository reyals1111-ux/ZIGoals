# Today widgets and Goal creation evidence
- Scope: SS08/WID01–09 and MOB04 implementation; Goal type cards/progressive fields/review under SS04/GOAL01–03. WID10 cross-device acceptance is still pending authenticated durable sync/device proof.
- Settings are schema-versioned configuration only at `zigoals:settings:v1`, through `usePrivateStore`; stable Goal/Habit/Position IDs and metric choices resolve live canonical data, never copied balances.
- Balanced, Wealth, Habits + Health and Health-only presets preview before applying; first-run interest selection needs no account or wallet. Health-only omits financial modules and automatic market requests.
- Customization supports add/edit/remove/hide/show/size/order with explicit accessible buttons, native dialog Escape/focus restoration, live save feedback and focus preserved after reorder; deletion keeps the underlying record.
- Invalid/future settings, duplicate bindings and stale edits fail closed; unavailable/archived entities remain explicit. USD/EUR remain separate, missing valuation is unknown, Health uses its journal timezone and canonical water summary. Allocation deficits stay visible across all presets.
- Goal creation now uses Quantity/Value/Reward/Project choice cards, saved asset identities plus explicit manual symbols, USD/EUR currency choices, relevant fields and live preview; incompatible type changes require confirmation and preserve name/category/date/notes. Existing save/validation/allocation authority is retained.
- Test-first: settings/metrics initial suite failed before implementation; creator radio regression failed before implementation; integrity regression failed before selector addition. Final focused unit suite: 8 passed in 2 files (Node 24.19.0, pnpm 11.19.0).
- Browser: `run10-widgets.spec.ts`, `run10-goal-creator.spec.ts`, `unified-goals.spec.ts`: 24 passed, desktop + mobile Chrome, server 3110; explicit widths 320/360/390/430/768/1440, reload, edit/hide/reorder/remove, four preset previews, dialog Escape/focus, Health-only and financial creation/retry semantics. Provider routes are mocked in widget tests; no live-budget acceptance claim.
- Exact progress arithmetic remains asserted via accessible `aria-valuenow`; historical visual decimal-label assertions were updated for the approved rounded presentation. Older creator suites changed Goal-type interactions to radios and grouped-value expectations to the approved display; accounting assertions remain. Final secondary creator integration suite: 20 passed after formatting updates and the catalog coalescing fix.
- Full TypeScript check and scoped ESLint passed; lint retains the pre-existing custom-pages-path warning. `git diff --check` passed. Final logs: `.superpowers/run10/widgets/{unit-final,typecheck-final,lint-final,browser-final}.log`.
- Screenshots generated under `/private/tmp/run10-widgets-goals-final`; reviewed desktop Balanced and 320px Goal cards. Fixed an observed legacy destination CSS rule creating an implicit second Today column; explicit page grid and mobile padding retain usable content without clipping.
- Files: `apps/web/app/app/page.tsx`, `components/platform/today-intelligence.tsx`, `components/dashboard/{today-dashboard.tsx,dashboard.css}`, `components/{unified-goal-wizard.tsx,goal-creator.css}`, `lib/dashboard-{settings,metrics}{.ts,.test.ts}`, `tests/{run10-widgets,run10-goal-creator,unified-goals,wealth,run9,run9-1,coherence}.spec.ts`, this evidence.
- Read: full `docs/run10/MASTER_PROMPT.md`; `apps/web/AGENTS.md`; installed Next client/server guide excerpt; original Today/creator components; canonical GoalSummary, wealth, positions, Habit and Health selectors; new Health water/timezone APIs; relevant existing creator/browser tests. Backups: `.superpowers/run10/widgets/backup-20260923T200513Z` and timestamped `goal-tests-backup-*`.
- Remaining: real-device screenshots, full WID10 account/encrypted-sync/backup acceptance, populated-dashboard performance benchmarks, drag gesture behavior (explicit buttons are provided), and broader unified asset-picker polish are not claimed complete. No deployment, merge, transaction/signature or provider-key change.
