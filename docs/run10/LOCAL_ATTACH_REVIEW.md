# Bounded local-to-account attachment review
- Scope: uncommitted `local-attach.ts`, `local-account-attach.tsx`, `prepareAttach`/`confirmAttach` integration, their focused test assertions, and committed `8bde332` pending-policy guard. Source review only; no tests, providers, real owner data or implementation edits were exercised by this reviewer.
- No confirmed new blocking data-loss, cross-account write or Health-consent defect found in the inspected flow. This is not full AUTH-11 acceptance or a broad security audit.
- Explicit selection and Health checks are enforced in the planner (`local-attach.ts:7–8`) and confirmation (`vault-sync-controls.tsx:63`); unselected raw domains are omitted from the plan. Both account-local and decrypted cloud sections must be absent or schema-equivalent to the empty domain (`local-attach.ts:11`).
- Preparation captures only selected normal Local domains, creates an encrypted selected-data backup, and publishes the preview after an account-generation fence (`vault-sync-controls.tsx:53–56`). Confirmation re-captures and compares reviewed source, re-reads account/cloud emptiness, then invokes storage-fenced apply (`:65–68`). It never deletes the Local source. Reading a legacy source may migrate its representation; exact pre-migration bytes are retained by the existing durable recovery transaction.
- UI confirmation requires saving the separate recovery secret, requesting the encrypted download, and explicitly confirming the file was saved (`local-account-attach.tsx:10–12`). Download initiation is not proof of disk persistence; the final checkbox is user attestation. Lock/account change and Health-consent change clear the preview and its secret.
- Corrected interruption finding, by follow-up source review: `account-data.ts:24–40` acquires every selected domain lock in a stable sorted order, validates every captured value/revision, then sends all changed sections to `database.ts:54–84` in one IndexedDB transaction covering records, headers, outboxes, receipts and recovery. A later conflict or pre-commit fence failure aborts the entire imported batch; another tab cannot observe/upload only an earlier imported section from that failed batch. Empty-domain pointer initialization occurs before the batch and can survive, but does not contain imported records.
- Inventory correction source-checked: `local-attach.ts:15` now traverses object-array rows and aggregates nested collection counts at the same field path, including Habit check-ins/rules, without exposing record IDs in labels. This remains a collection-count preview, not independent reference-integrity verification.
- Cloud concurrency limit: destination emptiness is rechecked before local apply, but a different device may populate cloud state afterward. The subsequent normal synchronization reconciles or conflicts against the latest cloud state; this is not a server reservation guaranteeing an empty destination throughout attachment.
- Pending-policy correction inspected: `cloud-sync.ts` stamps newly journaled operations with policy 2 and rejects absent/older/newer policy before replay. The rejected queue is preserved rather than sent or silently discarded; new-policy retry/idempotency uses the original operation ID. Recovery/migration UI for incompatible pending work remains separate unfinished work.
- Tests inspected cover selected encrypted roundtrip, populated-section refusal, Health exclusion and changed-source refusal; pending-policy assertions cover absent and future policies with no transport write. Root owns browser integration, types/lint and test execution. No hosted activation, PR readiness or milestone completion follows from this review.
- Lock/regression follow-up: no deadlock or new data-loss defect found in the inspected correction. Pointer initialization releases its individual locks before sorted nested acquisition; inspected callers acquire account-sync before domain locks, with no reverse acquisition found. The single-domain `commit` wrapper preserves immutable pre-await capture and original receipt/recovery semantics; hashing occurs before the active IndexedDB transaction.
- Follow-up verification limits: database assertions were read, not rerun. The later-domain revision-conflict case checks earlier records/outboxes roll back; `database.test.ts:42` now throws on fence call 4, after first-domain headers/outbox are staged and before the second domain starts, then asserts both domains/outbox remain absent. Root reports the 16.89s browser pass including downloaded-backup decryption, copied Habit on the second profile and sign-out returning to untouched Local records; this reviewer did not rerun it. Root's latest whole-unit report has 1337 passes and four Miniflare checks awaiting a permitted loopback rerun, so no full-suite pass is asserted here. Cloud publication and journal acknowledgement remain separate from atomic local apply.
