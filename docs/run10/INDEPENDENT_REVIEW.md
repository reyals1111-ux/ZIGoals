# Independent follow-up review — Run #10

- Outcome: **PARTIAL — REQUIRED_GAPS_REMAIN**. This is a focused code and regression review, not a full security audit, hosted deployment certification, or completion of all acceptance journeys.
- Scope: entire authoritative MASTER_PROMPT.md and REQUIREMENTS.md read; root-owned storage, encrypted backup/sync, account adapter, food lookup and their integration call sites inspected. This follow-up rechecked the four reported fixes; shared visuals/ecosystem were authored by this reviewer and are not presented as independently reviewed.
- Fresh verification: Node 24.19.0; food-lookup, vault/local, vault/backup and server/private-account unit suites: **12 passed across 4 files**. Desktop Playwright run10-private-vault + run10-barcode: **7 passed**, local fictional data only; log `/private/tmp/run10-independent-review-browser.log`. No real account, email, owner data or upstream food service was exercised.
- Resolved food basis finding: parser preserves `unverified-100g-or-100ml`; preview discloses ambiguity and unknown nutrients; checkbox gates both button and log handler, resets on lookup, and requires package evidence or a private correction to per-100-g values. No liquid density is inferred. Browser test verifies the unchecked gate and reviewed private snapshot; it does not prove provider correctness.
- Resolved open-tab notification finding: usePrivateStore publishes only the module key through BroadcastChannel and refreshes active non-Showcase readers; channels close on cleanup. Browser test verifies writes in both directions between already-open tabs. Browsers lacking BroadcastChannel still need a reload for durable-store changes; this is not cross-device sync proof.
- Resolved durable restore ceiling finding: ModuleBackup checks byte size against store.importLimit; durable selection permits 32 MB and validates the domain schema without the legacy 2 MB parser. Browser test restores and exports 6,000 real-schema Health diary entries above 2 MB with exact JSON value equality. Other domain caps and database row/index limits still apply.
- Resolved schema-gated export finding, by source inspection: exportDurableStore reads stored database values without applying the current application schema, and usePrivateStore uses that export path. This exports reconstructed JSON values, not original serialized whitespace or an unreadable database; no dedicated malformed/future-schema export regression test exists yet. Migration recovery bytes remain separate.
- No additional confirmed regression found in the four-fix follow-up. A separately authorized ecosystem correction restored the explicit sentence “External investment and funding integrations are disabled.”; ecosystem-ui was red before the edit and **3 tests passed** after it, with assertions unchanged. No other application source was edited during this review.
- Required account/sync work remains: no account UI, integrated account-scoped app persistence, outbox transport loop, or independent-browser account journey; pullVault/prepareOperation have test callers only and local persistence uses the `local` scope. Real OTP, account deletion, device/server session revocation, key-epoch lifecycle and financial aggregate conflict handling are not established by adapter/worker fixtures. Owner setup alone cannot finish these implementation gaps.
- Required capacity work remains: positions.ts retains 240-item history limits and goal-intelligence.ts a 600,000-byte history budget; database splitting is at top-level arrays, so a nested habit can still hit the 250 KB row limit. The Health roundtrip does not establish DAT-11 across domains, paged application reading, sustained performance, or the complete fault matrix.
- Other acceptance limits remain in their slice evidence: official ecosystem logos use labeled initials fallbacks, one social candidate is unresolved, contextual ecosystem placement is unfinished; full motion/video, physical-phone keyboard/browser-zoom and performance evidence is not supplied by these focused checks. See VISUAL_EVIDENCE.md, ECOSYSTEM_COVERAGE.md and STORAGE_SYNC_EVIDENCE.md.
- Page inventory (existence only): `/`, `/app`, `/app/activity`, `/app/ecosystem`, `/app/goals`, `/app/goals/[id]`, `/app/goals/new`, `/app/goals/positions`, `/app/goals/tracked`, `/app/goals/tracked/[id]`, `/app/habits`, `/app/health`, `/app/markets`, `/app/settings`, `/app/wealth`, `/app/wealth/asset/[id]`.
- API inventory (existence only): `/api/food-lookup`, `/api/market-assets`, `/api/market-history`, `/api/market-insights`, `/api/market-logo`, `/api/market-quotes`, `/api/positions`, `/api/private-account`. Route existence is not permission-boundary or acceptance coverage.

## Subsequent bounded encrypted-sync review

- Scope: independent source review of `apps/web/lib/vault/cloud-sync.ts`, `crypto.ts`, `workers/private-sync/worker.mjs` and relevant caller/storage-lock code; reviewer added only `cloud-sync.test.ts`. Implementation fixes were made by the integrating agent. Earlier observations above describe the earlier checkpoint; this follow-up does not certify remaining acceptance journeys or a hosted service.
- Confirmed rollback/fork finding: trusting only the unsigned transport revision allowed an older encrypted HEAD to appear newer. The implementation now persists authenticated HEAD revision and ciphertext digest high-water marks; regression tests reject older and same-generation forked HEADs even with a fabricated newer outer revision.
- Confirmed merge finding: lexicographic sorting of identity arrays changed a shared existing order during unrelated edits. Tests now verify preserved common order, a sole reorder, conflict on differing simultaneous reorders, divergent financial edits, and edit/delete conflicts without input mutation.
- Confirmed journal race finding: concurrent tabs could overwrite pending-operation state or regress the watermark. Source inspection confirms an account-scoped Web Lock now encloses capture, synchronize, apply and commit; missing Web Locks fail closed. **Independent browser proof of concurrent-tab sync/locking remains outstanding.**
- Confirmed Health replay finding: a previously unsent Health operation could replay after transfer permission was withdrawn. The new regression initially failed (8 passed, 1 failed); the fix persists `pendingHealth` and also checks pending row domains before replay. Health-disabled snapshot and publish tests pass; caller inspection also confirms consent/recovery-secret cleanup on account changes.
- Fresh focused verification: pinned Node 24.19.0, `cloud-sync.test.ts`: **9 passed across 1 file**, independently rerun; log `.superpowers/run10/market/cloud-review-final.log`. Coverage includes a snapshot above 1 MB with staged chunks and HEAD published last, lost-ack same-operation replay, rejected-409 journal cleanup preserving the previous HEAD, initial-attach conflict/empty-device pull, Health exclusion, rollback/fork rejection and merge conflicts. These are mock-transport/real-crypto tests, with no hosted account, provider traffic or owner data; the integrating agent's wider 25-test run was not independently rerun here.
