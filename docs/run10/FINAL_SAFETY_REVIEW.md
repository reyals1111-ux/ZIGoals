# Final bounded persistence/sync safety review
- Scope: read-only production review of `vault/local.ts`, `database.ts`, `account-data.ts`, `cloud-sync.ts`, `use-private-store.ts`, account/storage/transport fences, and positions/Habits/Health/dashboard schemas against exact DAT-01–11 and SYN-01–16 specifications in REQUIREMENTS.json; earlier ACCOUNT_INTEGRATION_REVIEW.md and INDEPENDENT_REVIEW.md findings were checked to avoid duplicates.
- **P1 found and corrected during integration — accepted financial evidence could be overwritten by sync.** Before correction, `account-data.ts` validated only the resulting schema; a remote/imported snapshot changing an existing income event from 10000 to 90000 under the same ID passed `applyData` and replaced the durable record. A unilateral finance replacement also passed `cloud-sync.ts` when the other copy matched its base. This violated SYN-06 independently of whether financial aggregate shape remained valid.
- Regression evidence: the assertion now at `apps/web/lib/vault/final-safety-review.test.ts:28` originally failed because `applyData` resolved rather than rejecting; fixture uses actual platform schema, durable migration/export and fake IndexedDB, with fictional data only. The initial fixture comparison was corrected to use exported durable serialization before this decisive reproduction.
- Correction inspected: `apps/web/lib/vault/account-data.ts:13` retains financial evidence, contributions and retained history by identity/content; `:23` validates the entire incoming snapshot against captured data before the first domain commit. `apps/web/lib/vault/cloud-sync.ts:72` checks the merged snapshot against journal base, remote and local accepted copies before staging new encrypted writes. The integrating agent authored these production changes.
- Explicit backup replacement remains separate: schema-only validation without a sync prior can accept a replacement, while sync cannot silently propagate the replacement over retained immutable evidence. Legitimate appended void/correction evidence remains valid; UI Goal-lock checks were not reused as sync policy.
- Fresh focused verification: pinned Node 24.19.0, `pnpm exec vitest run apps/web/lib/vault/final-safety-review.test.ts` — **3 tests passed**. Proof covers refused local apply, refused mutation/removal, allowed appended void, and real-crypto/mock-transport rejection before any new cloud write with unchanged journal state.
- No additional confirmed new accepted-data-loss, future-schema down-conversion, stale-account cache publication or Health-permission defect was found in this bounded pass. This is a limited review result, not proof that all such races are absent.
- Source observations: supported domain schemas reject unknown/future fields; migration retains exact original source before pointer publication; durable writes use revision CAS with atomic outbox storage; account-scoped views guard generation on access; captured outbox acknowledgements remain scoped to their original account/operation.
- Existing gaps are not reclassified as new findings: multi-domain apply/journal/acknowledgement interruption, incomplete lifecycle/delta/capacity acceptance, cross-device conflict/recovery journeys and hosted/physical-device proof remain outside this pass. Concurrent helper changes to financial method, market and UI were not independently re-certified here.
- Replay limit: `cloud-sync.ts:71` replays a pre-existing pending operation before the new snapshot-retention checks at `:72`; no mixed-build pending-operation migration/revalidation was tested. New-publication regression proof must not be described as revalidating every operation prepared by older builds.
- Review changes only: this report and the uniquely named focused regression file. No broad suite, real owner data, account provider or production migration was exercised. This report does not mark Run10 or the consumer milestone complete.
- Follow-up source inspection: `local.ts:21` now schema-parses empty defaults before migration, matching validation serialization; `goal-intelligence.ts:104/110` rejects capacity instead of pruning, and `asset-management.ts:20/29/34` retains all accepted asset events under schema ceilings. No new data-loss defect found in those changes; larger caps alone do not establish full DAT capacity/paging acceptance. Associated focused test assertions were read, not rerun by this reviewer.
- **P2 follow-up found and corrected — actual offline water receipt arrays conflicted.** `health-daily.ts:125` adds string receipt IDs omitted by the simplified optional-group fixture. Source recheck confirms `cloud-sync.ts:45–58/63` now prechecks both explicit Health receipt sets before generic equality shortcuts, refuses prior-receipt removal, and deterministically unions new receipts without changing arbitrary array policy. Real `addWater` fixture verifies 250+500 ml, replay idempotency and unilateral/identical receipt-removal rejection. The integrating agent reports 14 focused tests passed; this reviewer inspected source/assertions without rerunning them. Extended browser evidence remains owned by integration; no additional material issue found in this bounded correction review.
- Final integration source review, `8924625..2bf6f91`: no new material defect or weakened financial/persistence safety assertion found in the inspected delta. Today recovery links (`today-dashboard.tsx:58`) expose only readable legacy records while retaining the private-store error; Health QuickAdd (`health-app.tsx:67–69`) prioritizes the meal selector; typography changes enlarge functional text. Changed tests retain exact amount/ARIA values, ownership, private-byte/recovery and no-financial-action assertions; widget reload now waits for save-dialog closure. Programmatic ledger comparison retained all **290** IDs with unchanged specification, acceptance criteria, delivery class, scope hint and dependencies; journey shard retains all **40** original journeys/specifications and incomplete acceptance. Helper product rows remain unverified/in progress, core lifecycle/delta/paging gaps stay explicit, and no owner-accepted/released flag is true. No tests or implementation edits in this pass; reported **219 passed / 77 failed / 16 skipped** remain historical failures/evidence, and the affected **154-check** rebuilt rerun was still running when assigned. This source review does not certify that rerun, physical/hosted acceptance, activation, or PR readiness; no activation or draft-state change is authorized by it.
