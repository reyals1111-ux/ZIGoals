# Integration corrections and retained evidence

1. Independent account review found transient auth/registry failures cleared the session cookie. Non-auth failures now return503 without deleting it;8 failure combinations are tested. A final generation fence after the last outbox acknowledgement prevents an old session from publishing a success status after lock.
2. Independent financial review reproduced stale same-ID completeness approval and a12.342s rational-arithmetic stress case. Content-bound reviews, cross-cancelled factors and explicit work bounds fix these cases; see FINANCIAL_METHOD_REVIEW.md. Sync-only immutable-evidence transition guards prevent accepted finance from being replaced under its old IDs; explicit backup replacement stays separate.
3. The four-domain actual-browser journey exposed empty-device migration comparing unparsed default serialization against schema-canonical serialization. Empty initial records now pass the schema before staging; a red→green test initializes all4 real domains. Multi-domain local browser sync then passed10.78s.
4. Original history limits silently evicted older financial observations and asset events. New limits refuse additional data instead. Red tests observed pruning before the change;28 Goal-intelligence and3 independent sync safety tests pass, plus19 prior lifecycle/accounting tests after their two eviction expectations changed to retention. This assertion change implements the master retention requirement; all archived-period totals and conservation assertions remain.
5. Earlier CI web job35920615735 failed on two test-harness React children-prop lint errors; these were corrected without changing diagnostics assertions. Contract job passed. Full integrated local suite after corrections:1331 passed,1 explicit browser-opt-in skip; lint0errors/2ref-cleanup warnings; full typecheck passed. Later source/build verification is recorded separately.
6. Expanded offline browser work remains subject to its final result. Failures from an unavailable devserver, duplicate metric fixture, and navigation before loaded/saved UI are retained in local logs rather than relabeled as passes. Fixture authentication is not real OTP or physical-device proof.

Offline Health correction: first-created optional groups and the two append-only operation receipt sets now reconcile; prior receipts cannot be removed through fast equality paths. Real addWater red→green test proves250+500ml, replay idempotency and receipt retention. Extended two-browser Worker journey passed14.80s after correction; source independently inspected.
