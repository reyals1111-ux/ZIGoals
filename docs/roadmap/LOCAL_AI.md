# Local AI companion workstream — deferred

Hermes and RTX5090 can later support private drafting, content analysis, classification, summaries, idea clustering, synthetic test fixtures, documentation, media experiments and research preprocessing. This is separate from the financial app and has no Milestone1 integration.

Local model output is never authority for live network parameters, protocol messages, custody or accounting. Verify against primary sources and deterministic tests. Future AI proposes structured plans, deterministic policy validates, user reviews and user signs. AI never holds keys, receives mnemonics, bypasses policy or silently executes financial actions.
