# M4 final integrated architecture/security/code review
Scope: merged main 7d354e3c72fa671abc100facf908b247fb992cc0 through 97ef3b5; supplied diff, user specification, plan/ledger, current implementations, runbooks and recorded evidence; read-only checkout, no suite reruns or subagents.
### Strengths
- Canonical bytes/schema/source-tree/lock/epoch/validator checks feed preparation before network access; single/developer builds and upload-approved states are rejected (scripts/release/verify.mjs:30; scripts/prepare-deployment.mjs:11; scripts/lib/prepare-deployment.mjs:6).
- Fresh independent build jobs use read-only PR permissions; exact-main issuance separates verification from the OIDC job, with no repository execution under attestation authority (.github/workflows/reproducibility.yml:8; .github/workflows/release-candidate.yml:75).
- Public/invalid modes guard quote/execute and actual sign/broadcast; nonce HTML is dynamic/no-store; RPC integers/ownership/cursors are bounded; diagnostic scope remounts by chain/account (apps/web/lib/wallet.ts:134; apps/web/middleware.ts:4; apps/web/lib/rpc-response.ts:23; apps/web/app/app/settings/page.tsx:96).
- Isolated Alpha config has no apex/domain/mail mutations; docs distinguish unsigned PR reproducibility, preparation, owner approval, absent live hosting/Keplr/attestation and future chain deployment (apps/web/wrangler.alpha.jsonc:3; docs/deployment/VERIFY_RELEASE_ARTIFACT.md:3; docs/verification/m4/PUBLICATION.json:2).
### Issues
Critical: None. Important: None.
Minor M4-FINAL-01 — apps/web/app/layout.tsx:9: metadataBase is only a URL-resolution base and does not emit a canonical link; no alternates.canonical is supplied, leaving user specification §18 incomplete. Prepare an appropriate canonical public entry URL using the verified served origin, without private goal identifiers, and assert the rendered link. Installed Next already derives Twitter metadata from OpenGraph, so absence of an explicit twitter object is not a separate defect.
Minor M4-FINAL-02 — docs/RUN_4_REPORT.md:103: the copied-diagnostics claim includes chain/deployment, wallet yes/no and a bounded browser class, but apps/web/lib/diagnostic-summary.ts:9–14 emits build mode/version/commit, session label, RPC/REST status and timestamp only. Narrow the report to those actual fields; this is evidence accuracy, not a privacy leak.
### Recommendations
Close the two nonblocking minors and append actually observed final hosted results/review status. Keep protected apex Google Fonts explicitly separate from local Alpha assets; do not modify apex. Final hosted checks remain a completion gate, not a source-review finding.
### Assessment
Ready for owner merge? YES from architecture/security/code review, subject to the normal successful final hosted checks; the two minors are nonblocking. Recorded clean-source evidence is 476 JS/25 Rust/36 Next-browser/8 workerd plus full Chrome restart, not independently rerun by this reviewer. No material integrated defect was identified; this verdict grants no public deployment, attestation, signing, chain upload, mainnet or funds approval, and real hosted HTTPS/Keplr/Free CPU checks remain unverified.
