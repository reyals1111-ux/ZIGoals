# Finance correctness review
- P1 — `apps/web/lib/positions.ts:103–107`: RESEARCH_ONLY is excluded before the VALUE branch, but a matching valuation overwrites zero and contributes to verified progress. Reproduced with a RESEARCH_ONLY VAULT and a VERIFIED USD valuation: current=10000, verified=10000, 100% progress. Apply the eligibility guard after valuation (or skip ineligible sources); add a regression test.
- P1 — `apps/web/components/platform/positions-view.tsx:21–22`, `tracked-detail.tsx:35`: editing retains a manual Position ID while allowing asset changes and forcing decimals=18. Existing allocations and historical snapshots retain their old raw units. Changing ZIG to BTC relabels old snapshots as BTC; editing an imported six-decimal Position changes allocation interpretation by 10^12. Keep asset/denom/decimals immutable for an existing ID and parse edit quantities using its original decimals, or explicitly create a different Position.
- P2 — `apps/web/lib/positions.ts:101`, `apps/web/components/platform/positions-view.tsx:18`, `common.tsx:7`: sync becomes CURRENT on successful reads but is never aged or marked failed; valuation observedAt is also ignored. Reproduced a six-year-old CURRENT verified snapshot/valuation yielding requiresReview=false. A failed refresh preserves CURRENT, while Goal cards/Today omit timestamps. Derive stale/review state from explicit freshness policy and expose failed/old observation status without discarding the snapshot.
- P1 — `apps/web/lib/native-positions.ts:20–23,39–44`: bank/delegation/reward/unbonding requests read independent latest states; there is no pinned block height or response-height check (also true for subsequent pages). An undelegation between reads can return old stake plus new unbonding, or completed unbonding plus new liquid balance, counting the same wealth twice under distinct Position IDs. Acquire one supported chain height, pin all snapshot queries, and fail closed if the endpoint cannot prove a coherent observation; test a mixed-height fixture.
- Earlier past-contribution double counting was reported to parent; parent added an as-of boundary during review, so it is not listed as an outstanding defect here.
- Verification: selected positions/native-positions/private-storage suite passed, 3 files / 25 tests. Two direct Node reproductions confirmed research-value inclusion and never-aging CURRENT state; manual edit issue follows the editor mutation and history renderer exactly.
- Reviewed per-network integer denomination handling, allocation conservation/deficit rounding, closed allocation release, private-storage failure preservation, and reader authority/egress boundaries. No additional material defect found in those paths. Review covers the current working files, which parent continues modifying.
