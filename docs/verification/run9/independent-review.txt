Run 9 independent review — three findings reported and fixes inspected
1. [P1, fixed in source] Positions editor dropped marketRef, valuationMode and quoteCurrency on ordinary quantity/name/note edits. Read-only reproduction originally changed bitcoin/EUR requests to [] and removed marketRef. The new manualPositionFromEditor preserves existing metadata and explicitly applies/removes manual valuation overrides.
2. [P2, fixed in source] uniqueMarketRequests imposed a 500-entry bound before deduplicating. Read-only reproduction originally threw on 501 identical bitcoin Positions. Updated helper accepts the complete supported portfolio, deduplicates, and uses a 2000 unique-pair bound; client transport chunks requests, and cache count was raised consistently.
3. [P2, fixed in source] contributed Goal deletion unlinked Habits before immutable-history rejection. Updated handler performs the protected platform deletion first; Delete is disabled for Goals with financial history. Refused deletion now leaves Habit links alone.
No further concrete credential leakage, malformed-provider replacement defect, or correctness blocker found in the reviewed scope.
Root owns full build/browser/regression checks. This reviewer performed source inspection and focused read-only reproductions only; no project source edits, commits, deployment, or external actions.

Later artifact scan discovered OpenNext's compiled environment contained the server key; no client asset contained it. This was outside the initial source-only review. The build now strips that runtime credential, checks residual artifact copies and clears the production build cache. Independent follow-up review found no actionable defect and confirmed the installed adapter's environment format and runtime-binding precedence. Root reran the Alpha build, scan and security gate.
