# Run11 source-bound evidence

Starting source `3ca2f42303724ef1317aded6982c9fdd6fd8775d`. Baseline focused browser: 14 planned, 12 pass, 2 fail (the 13.44px captions); `.superpowers/pre11b-browser/2026-09-26T18-19-47-681Z-88110/summary.json`. Caption-only application delta later committed as `5d2143a`: 16/16 desktop/mobile product+visual cases passed, no skip, `.superpowers/pre11b-browser/2026-09-26T18-21-04-623Z-88405/summary.json`. Build recorded preparation HEAD with the explicitly identified uncommitted caption delta; do not treat this as a final clean-source Run11 run.

Working-source crypto/rotation/account tests: 31/31 across crypto, cloud-sync, private-account adapter, persistent rotation; positive-integer epochs retain v1 compatibility. Separate rotation client fault test preserves active catalog after lost staging acknowledgement, validates resumed ciphertext and activates after restart. Two real Chrome profiles: `RUN10_BROWSER=1 RUN11_REVIEW_ORIGIN=http://127.0.0.1:3112 fnm exec --using 24.19.0 pnpm exec vitest run scripts/run10/account-browser.test.mjs` passed 1/1 (19 seconds) at working source. Journey covers protected Local attachment, Goal/Habit/Health/widget sync, offline independent water additions, rotation UI, wrong old secret, correct new secret, session revocation and Local return. HTTP route is intercepted into the actual production adapter; persistent Worker and WebCrypto/IndexedDB are real. This is not the full packaged app or real identity/email/physical-device proof.

Account runtime binding test: 1/1; private-account adapter: 13/13 after refresh changes. Typecheck passed at that working source. Later exact committed-source rerun still required. No counts above are added into a fictional full-suite total.

Reviewed recovery corrections: independent reviewer identified five concrete defects; added failing tests, fixed session-family revocation, identical refresh replay, post-commit rotation acknowledgement recovery, deletion authorization after erasure, and provider deletion job recovery without deleted credentials. Persistent runtime suite: 9/9 across rotation/lifecycle/session files. Actual stopped SQLite snapshot restore reproduces then rejects stale-vault resurrection. Both fixture Workers now explicitly intercept all outbound requests into synthetic callbacks; one auto-review command rejection was resolved by adding and proving that isolation, with no live request made. Account two-browser suite then passed 1/1 in 18.44 seconds including required encrypted pre-deletion export and terminal cloud denial. Default identity/provider responses remain controlled fixtures.

Checkpoint full unit run (working source after f82027c): 130 files passed, 1 opt-in file skipped; 1,408 tests passed, 1 skipped. `/tmp/zigoals-run11-unit-checkpoint.log`. Opt-in account browser subsequently 1/1 passed in 18.44 seconds, separately reported, not added to that total.
