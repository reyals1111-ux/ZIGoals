# Task 3 — Journal and multi-tab hardening
- Owned implementation: scoped Web Locks serialize local ledger writes, metadata saves/imports, and testnet confirmations; unsupported lock environments refuse writes.
- Reviews capture raw ledger/metadata or scoped durable journal revisions; confirmation checks inside the lock reject missed-event races before mutation/signing.
- Native scoped storage events reload active local views/plans; external journal notifications carry chain/wallet/contract/document identity and invalidate matching reviews without cancelling the originating tab.
- Metadata recovery/import refuses future numeric schema versions without writing any key; damaged v1 data retains exact quarantine behavior; stale expected revisions refuse overwrite.
- IndexedDB transition scans signed hash claims atomically in the write transaction, including future-schema/lowercase claims, without schema migration or deletion.
- Journal time plausibility (created ≤ updated ≤ local clock + 5 minutes) warns and preserves suspicious rows; exported recoveryCandidates excludes these rows and prioritizes recent eligible receipts (wallet integration owned by controller).
- Local history validates canonical, nonnegative, plausibly current, chronological timestamps before any write; impossible history fails closed with stored bytes intact.
- Review copy now promises saving only after confirmation; failed plan storage explicitly reports the created goal's missing plan. Both mode labels state “Mode: this tab”.
- TDD evidence: /tmp/m3-task3-red.txt (7 expected module failures), /tmp/m3-task3-provider-red.txt (3 expected stale-review failures), /tmp/m3-task3-copy-red.txt (premature-save copy failure).
- Browser baseline evidence: /tmp/m3-task3-browser-red.txt (3101: stale funds dialog, stale recovery title, missing mode scope each fail); initial sandboxed Chrome launch failed before tests and was rerun successfully with authorized escalation.
- Final verification: 120 unit/provider tests, full typecheck, owned ESLint, and production build pass. Browser run covered all 28 desktop/mobile cases: initial 22 passed; correcting ambiguous test selectors and awaiting actual async import completion yields multitab 10/10 and lifecycle 2/2. Logs: /tmp/m3-task3-{unit-green,typecheck,lint,build,browser-green,multitab-green,lifecycle-green}.txt.
- Five real two-tab regressions cover sequential funds/review cancellation, simultaneous confirmations, recovery replacement, independent mode, and scoped journal transport/retained future bytes. Browser testnet adapter cannot sign; deployment-disabled baseline does not permit a real financial review, covered by provider integration with real IndexedDB/BroadcastChannel.
- Limits: locks coordinate participating same-origin tabs only (reload older application versions); browser/local clock is not trusted time; first terminal receipt remains final with no reorg/quorum proof; abrupt shutdown/eviction and legacy corruption remain visible uncertainty, with no rebroadcast/indexer added.
- Backups: /tmp/zigoals-m3-task3-backup; owned implementation committed separately from controller integration. Production review server remains on 127.0.0.1:3102 (session 13504); controller owns shutdown.
